{
  "name": "jobpilot",
  "private": true,
  "scripts": {
    "build": "pnpm -C backend build",
    "lint": "pnpm -C backend lint",
    "test": "pnpm -C backend test",
    "test:e2e": "pnpm -C backend test:e2e",
    "migration:generate": "pnpm -C backend migration:generate",
    "migration:run": "pnpm -C backend migration:run",
    "migration:revert": "pnpm -C backend migration:revert",
    "migration:show": "pnpm -C backend migration:show"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
